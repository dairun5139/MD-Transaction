# MMD Transaction — 蒙东新能源电站交易 RAG 知识库

## 项目概况

面向蒙东地区新能源电站电力交易场景，构建基于 **RAG（检索增强生成）** 架构的本地化智能知识库系统。系统以蒙东电力交易规则汇编、交易员培训教材等文档为知识源，结合本地部署的 Ollama DeepSeek-R1 大模型，为交易员提供精准的政策解读、规则查询与决策辅助。

## 系统架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户交互层 (CLI)                              │
│                     interactive_qa() — 终端问答                       │
└─────────────────────────────────┬───────────────────────────────────┘
                                  │  question
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       RAG 问答链 (LangChain)                         │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────────┐ │
│  │   Retriever   │   │   Prompt     │   │   ChatOllama (LLM)       │ │
│  │  (ChromaDB)   │──▶│  Template    │──▶│  deepseek-r1:latest      │ │
│  │   top-k=4     │   │  (中文提示词) │   │  temperature=0.3          │ │
│  └──────────────┘   └──────────────┘   └──────────────────────────┘ │
└─────────────────────────────────┬───────────────────────────────────┘
                                  │  answer
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         推理结果输出                                  │
│                     StrOutputParser → 终端展示                        │
└─────────────────────────────────────────────────────────────────────┘
```

## 离线索引流水线（一次性/增量触发）

```
┌──────────────────────────────────────────────────────────────────────┐
│                        离线索引流水线                                  │
│                                                                      │
│  ┌──────────┐    ┌──────────────────┐    ┌──────────────────────┐   │
│  │ data/    │    │ Text Splitter    │    │ Embedding            │   │
│  │ *.pdf    │───▶│ RecursiveChar    │───▶│ BAAI/bge-small       │   │
│  │ *.docx   │    │ chunk=500        │    │ zh-v1.5 (CPU)        │   │
│  └──────────┘    │ overlap=50       │    └──────────┬───────────┘   │
│                  └──────────────────┘               │                │
│                                                    ▼                │
│                               ┌──────────────────────────────────┐   │
│                               │  ChromaDB (持久化)               │   │
│                               │  chroma_db/                      │   │
│                               │  ├── chroma.sqlite3              │   │
│                               │  └── *.parquet                   │   │
│                               └──────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **大语言模型** | Ollama + DeepSeek-R1 | 本地推理，无需联网，数据不外泄 |
| **AI 框架** | LangChain 0.3+ | RAG 链编排、Prompt 管理、文档加载 |
| **向量数据库** | ChromaDB | 轻量级，本地持久化，零配置 |
| **嵌入模型** | BAAI/bge-small-zh-v1.5 | 中文语义向量，HuggingFace 本地运行 |
| **文档解析** | PyPDF + docx2txt | PDF 和 Word 文档文本提取 |
| **文本分块** | RecursiveCharacterTextSplitter | 递进分割（段落→句子→逗号），适配中文 |
| **运行环境** | Conda + Python 3.10+ | 虚拟环境隔离 |

## 目录结构

```
（项目3）蒙东新能源电站交易AI算法开发/
├── data/                          ← 知识源（用户提供）
│   ├── 20251021010756789.pdf          PDF：交易规则/通知
│   ├── 蒙东新能源电站交易规则汇编_AI算法开发版.docx  Word：规则汇编
│   └── 蒙东电力交易市场交易员培训教材.pdf         PDF：培训教材
│
├── chroma_db/                     ← 向量库持久化目录（首次运行后自动生成）
│   ├── chroma.sqlite3                 元数据索引
│   └── *.parquet                      向量数据
│
├── rag_app.py                     ← RAG 核心应用（入口脚本）
├── requirements.txt               ← Python 依赖清单
├── MMD transaction.md             ← 本文档（项目架构说明）
│
└── all-in-rag/                    ← RAG 学习参考框架（已克隆）
    └── code/C1~C3/                    各章节示例代码
```

## 数据流详解

```
1. 文档加载
   PyPDFLoader / Docx2txtLoader
   → 按页/段落提取原始文本

2. 文本分块
   RecursiveCharacterTextSplitter (chunk_size=500, overlap=50)
   → 按 ["\n\n", "\n", "。", "；", "，", " ", ""] 递进切分
   → 保留块间重叠，避免语义断裂

3. 向量化存储
   BAAI/bge-small-zh-v1.5 → 512维向量
   → ChromaDB 写入 chroma_db/ 持久化
   → 后续启动自动跳过索引步骤

4. 检索增强
   用户问题 → 同模型向量化 → ChromaDB 相似度检索 top-k=4
   → 召回上下文拼接 Prompt → Ollama DeepSeek-R1 推理
   → 输出答案
```

## 安装与使用

### 前置条件

- Ollama 已安装并拉取模型：`ollama pull deepseek-r1:latest`
- Conda 环境已就绪
- 知识文档已放入 `data/` 目录

### 安装步骤

```bash
# 1. 激活 conda 环境
conda activate <your-env-name>

# 2. 安装依赖
pip install -r requirements.txt

# 如果 HuggingFace 下载模型慢，设置国内镜像：
# export HF_ENDPOINT=https://hf-mirror.com
```

### 启动问答

```bash
python rag_app.py
```

首次运行会自动完成：文档加载 → 文本分块 → 向量库构建（约 1-3 分钟，视文档量而定）。后续启动直接加载已有向量库，秒级就绪。

### 交互命令

- 输入问题直接回车，等待答案
- 输入 `quit` / `exit` / `q` 退出
- 按 `Ctrl+C` 也可以退出

## 关键设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 嵌入模型 | BAAI/bge-small-zh-v1.5 | 中文语义理解最优，体积小（~100MB），CPU 可运行 |
| 大模型运行方式 | Ollama 本地 | 数据不出本机，满足电力交易数据安全要求 |
| 向量知识库 | ChromaDB | 单机轻量，无需额外服务进程，SQLite 持久化 |
| Chunk 大小 | 500 字符 | 平衡召回精度与上下文完整性 |
| 检索条数 | top-4 | 在 DeepSeek-R1 32K 上下文中保留足够信息量 |
| LLM 温度 | 0.3 | 降低幻觉，保证规则类问答的准确性 |

## 扩展方向

1. **Web UI**：基于 Streamlit/Gradio 构建可视化问答界面
2. **对话历史**：引入 LangChain Memory 模块实现多轮对话
3. **混合检索**：结合 BM25 关键词检索 + 向量语义检索
4. **文档增量更新**：监控 data/ 目录变化，自动增量索引
5. **多模态支持**：扩展表格、图片等结构化数据解析
