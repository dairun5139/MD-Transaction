# 蒙东电力交易规则智能问答系统 — 交付说明

---

## 一、系统概述

本系统是一个**本地化电力交易规则智能问答工具**，将蒙东电力交易相关的 PDF/DOCX 规则文档构建为本地知识库，支持自然语言提问，自动检索依据并生成带来源追溯的回答。

**核心特点：**

- 🔒 **数据安全**：所有数据和模型均在本机运行，不上传外网
- 📎 **来源追溯**：每条回答附带来源文件名、章节标题、页码和片段摘录
- 🔍 **表格感知**：自动识别 PDF/DOCX 中的表格并转为结构化格式
- 🛡️ **隐私保护**：手机号、身份证、邮箱等敏感字段自动脱敏
- 🌐 **双模式**：支持浏览器 Web 页面和 REST API 调用

---

## 二、运行环境要求

| 组件 | 最低要求 | 说明 |
|---|---|---|
| 操作系统 | Windows 10/11 64位 | |
| Python | 3.10 或更高版本 | https://www.python.org/downloads/ |
| Ollama | 最新版 | https://ollama.com/ |
| 内存 | 16GB 或更高 | 模型运行约需 5GB |
| 磁盘 | 20GB 可用空间 | 含模型文件和知识库索引 |

---

## 三、首次使用步骤

### 步骤 1：安装 Python 3.10+

1. 访问 https://www.python.org/downloads/ 下载安装包
2. 运行安装程序，**务必勾选 "Add Python to PATH"**
3. 验证：打开命令提示符，输入 `python --version`，确认版本号 ≥ 3.10

### 步骤 2：安装 Ollama 并拉取模型

1. 访问 https://ollama.com/ 下载安装
2. 打开命令提示符或 PowerShell，运行：

```powershell
ollama pull qwen2.5:7b
ollama pull nomic-embed-text
```

3. 验证：运行 `ollama list`，确认两个模型已就绪

### 步骤 3：解压交付包

将交付包解压到任意目录（建议路径不含中文和空格，如 `D:\md_rag\`）。

### 步骤 4：启动服务

双击 `启动服务.bat`。

**首次运行会自动完成：**
- 安装 Python 依赖包（约需 1-2 分钟）
- 从 `data/` 目录构建知识库索引（约需 3-10 分钟）
- 启动 Web 服务并自动打开浏览器

**后续启动仅需几秒。**

### 步骤 5：开始使用

浏览器自动打开 `http://127.0.0.1:8000/`，输入问题后按 **Enter** 提交。

示例问题：
- "蒙东电力交易的中长期合约如何结算？"
- "市场主体注册需要什么条件？"
- "什么是偏差电量？偏差考核标准是什么？"

---

## 四、API 接口

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/` | Web 问答页面 |
| GET | `/api/health` | 健康检查 |
| GET | `/api/index/status` | 索引详情 |
| POST | `/api/ask` | 提交问题 |

**POST /api/ask 示例：**

```json
{
  "question": "蒙东电力交易的中长期合约如何结算？"
}
```

---

## 五、知识库更新

1. 将新的 PDF/DOCX 放入 `data/` 目录（或删除旧文件）
2. 删除 `faiss_index/` 目录
3. 重新双击 `启动服务.bat`

---

## 六、常见问题

**Q：提示"未找到 Python"？**
安装 Python 时未勾选 "Add Python to PATH"。重新运行安装程序选"Modify"并勾选。

**Q：提示"无法连接 Ollama"？**
从开始菜单启动 Ollama 应用，或运行 `ollama serve`。

**Q：回答不够准确？**
参考片段不相关 → 知识库中缺少依据。参考片段相关但回答有误 → 模型理解偏差。可删除 `faiss_index/` 重建索引再试。

**Q：端口被占用？**
关闭之前的服务窗口，或用 `uvicorn api_app:app --port 8001` 换端口。

**Q：首次启动很慢？**
正常，正在解析文档并构建向量索引，后续启动只需几秒。

---

## 七、技术支持

如遇问题，请提供：
1. 运行 `环境检查.bat` 的完整输出
2. 命令提示符中启动服务时的错误信息
3. 问题描述和期望回答

---

## 八、版本信息

- **版本号**：v1.0
- **日期**：2026-06
- **生成模型**：qwen2.5:7b
- **嵌入模型**：nomic-embed-text
- **Prompt 版本**：rag_prompt_v4_qwen
